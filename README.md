# FreshCart DevSecOps Lab

This repo is for the DevSecOps bootcamp. The code has security mistakes on purpose. Never use it in real life.

1. Click "Code", then "Codespaces", then "Create codespace on main".
2. Wait until the terminal says "Setup finished".
3. Open the Actions tab to see the scoreboard. Red means a problem is waiting for you.
