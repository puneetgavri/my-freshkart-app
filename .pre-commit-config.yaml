# Runs Gitleaks on every commit, before the commit is saved.
repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Gitleaks (look for secrets)
        entry: gitleaks git --pre-commit --redact --staged --verbose
        language: system
        pass_filenames: false
